# 🎸 Guitar Theory App — 전체 계획서 v3.0

작성일: 2026-06-06  
저장소: yamugyclaude/guitar-theory  
배포: GitHub Pages (정적 웹 호스팅)

---

## 0. 설계 원칙

1. **모든 기기, 웹 브라우저만으로 완전 동작** — 앱 설치 불필요, 서버 불필요
2. **탭 간 연동이 곧 앱의 핵심** — 각 탭은 독립 기능이 아닌 하나의 흐름 속 모듈
3. **백엔드·API 없이 100% 클라이언트 로직** — 음악이론은 규칙 기반 알고리즘으로 완전 구현 가능
4. **데이터는 사용자 브라우저에 저장** — IndexedDB(파일) + localStorage(설정·메타)
5. **오프라인 지원 없음** — 웹 접속 전제. PDF.js 등 외부 라이브러리 CDN 사용.

---

## 1. 프로젝트 개요

기타리스트 중급자 이상을 위한 **음악이론 분석 + 악보 관리 + 라이브 연주 도우미** 통합 웹앱.

**핵심 가치**: 탭 1에서 분석한 코드 진행이 탭 2(보이싱)·탭 3(솔로)으로 즉시 연동되고, 탭 6(악보 보관함)의 악보가 탭 7(코드차트)·탭 8(라이브 모드)으로 흐르는 하나의 통합 워크플로우.

---

## 2. 기술 스택

| 항목 | 내용 | 결정 이유 |
|---|---|---|
| 파일 구조 | 다중 파일 (index.html + CSS + JS 모듈) | 단일 파일은 수만 줄이 되어 유지보수 불가. GitHub Pages는 다중 파일 완전 지원 |
| JS 구조 | ES Modules (`<script type="module">`) | 파일 분리 + import/export 가능. 별도 번들러 불필요 |
| 상태 관리 | 중앙 AppState 객체 + 커스텀 이벤트 버스 | 탭 간 데이터 연동의 핵심. 라이브러리 없이 순수 JS로 구현 |
| 저장소 | IndexedDB (악보 파일 원본), localStorage (설정·메타데이터) | IndexedDB는 수백 MB 파일 저장 가능 |
| PDF 렌더링 | PDF.js (CDN) + 별도 `pdf.worker.js` 파일 | 단일 파일 구조에서의 Blob URL 트릭 불필요. 별도 파일로 깔끔 해결 |
| 다이어그램 | SVG 동적 렌더링 (순수 JS) | 외부 의존성 없음 |
| 배포 | GitHub Pages (`/docs` 폴더 또는 `main` 브랜치 루트) | push → 자동 배포 |
| 백엔드 | 없음 | 음악이론 로직은 전부 클라이언트 알고리즘으로 구현 |

---

## 3. 파일 구조

```
/
├── index.html              # 앱 진입점. 탭 레이아웃 껍데기만 포함
├── css/
│   └── style.css           # 전체 스타일 + 테마 8종 CSS 변수
├── js/
│   ├── app.js              # 앱 초기화, 탭 라우터, AppState, 이벤트 버스
│   ├── db.js               # IndexedDB 래퍼 (악보 파일 CRUD)
│   ├── theory.js           # 탭1: 음악이론 엔진 (키감지·다이아토닉·스케일 알고리즘)
│   ├── voicing.js          # 탭2: 코드 보이싱 DB + SVG 다이어그램 렌더러
│   ├── solo.js             # 탭3: 솔로 메이킹 로직
│   ├── analysis.js         # 탭4: 솔로 분석 데이터 + 렌더러
│   ├── library.js          # 탭5: 기타리스트 스타일 정적 데이터
│   ├── sheets.js           # 탭6: 악보 보관함 (IndexedDB 연동, PDF 뷰어)
│   ├── chart.js            # 탭7: 코드차트 에디터 + PNG 출력
│   ├── live.js             # 탭8: 라이브 모드 (셋리스트 + 풀스크린 뷰어)
│   └── settings.js         # 탭9: 설정 (테마·폰트·백업)
├── data/
│   ├── scales.js           # 스케일 정의 데이터 (모든 스케일 구성음·특성)
│   ├── chords.js           # 코드 구성음 + 보이싱 핑거링 DB
│   └── guitarists.js       # 5인 스타일 데이터 (TAB 포함)
└── workers/
    └── pdf.worker.js       # PDF.js Web Worker (메인 스레드 프리징 방지)
```

---

## 4. 탭 간 연동 설계 (핵심)

앱의 완전체는 **탭 간 데이터 흐름**으로 완성된다. 모든 연동은 중앙 `AppState`를 통해 이루어진다.

### 연동 흐름도

```
[탭1 이론 분석]
    │ 분석된 키·코드 진행 전달
    ├──→ [탭2 코드 보이싱]   : 코드명 자동 입력 → 다이어그램 즉시 표시
    └──→ [탭3 솔로 메이킹]   : 키·스케일 자동 입력 → 추천 스케일 표시

[탭3 솔로 메이킹]
    │ 선택된 기타리스트 스타일
    └──→ [탭5 스타일 라이브러리] : 해당 기타리스트 페이지로 이동

[탭4 솔로 분석]
    │ 분석된 솔로의 아티스트
    └──→ [탭5 스타일 라이브러리] : 해당 기타리스트 페이지로 이동

[탭6 악보 보관함]
    │ 저장된 악보
    ├──→ [탭7 코드차트]      : 선택한 악보 → 코드차트 에디터에 곡 정보 자동 입력
    └──→ [탭8 라이브 모드]   : 악보를 셋리스트에 추가

[탭7 코드차트]
    │ 완성된 코드차트
    ├──→ [탭6 악보 보관함]   : 코드차트 PNG로 저장
    └──→ [탭8 라이브 모드]   : 코드차트를 셋리스트에 추가

[탭2 코드 보이싱]
    │ 선택한 코드폼
    └──→ [탭7 코드차트]      : 코드차트 마디에 해당 코드 추가
```

### AppState 구조

```js
AppState = {
  // 탭1 → 탭2, 탭3으로 전달
  currentAnalysis: {
    progression: [],   // ["Am7", "D7", "Gmaj7"]
    detectedKey: "",   // "G"
    mode: "",          // "major"
    diatonic: [],      // [{ chord, degree, type }]
    scales: []         // 추천 스케일 목록
  },

  // 탭6, 7 → 탭8로 전달
  setlist: [],         // [{ id, title, type: "sheet"|"chart" }]

  // 탭 간 라우팅 트리거
  pendingRoute: null   // { tab: 2, payload: { chord: "Am7" } }
}
```

### 연동 UI 패턴

각 탭에서 연동 가능한 지점에는 **"→ 탭N에서 보기"** 버튼을 배치한다.  
버튼 클릭 시 AppState에 payload를 쓰고 해당 탭으로 이동. 해당 탭은 활성화 시 pendingRoute를 읽어 자동 반영.

---

## 5. 탭별 상세 기능

### 🎸 탭 1 — 이론 분석

**목적**: 코드 진행을 입력하면 음악이론적 맥락을 자동 해석

**입력**
- 코드 진행 텍스트 입력 (예: `Am7 - D7 - Gmaj7 - Cmaj7`)
- 코드 구분자: `-` `/` `,` 스페이스 모두 허용

**출력 (순수 JS 알고리즘)**
- 키 자동 감지: 입력된 코드셋이 가장 많이 일치하는 장조/단조 키 판별
- 다이아토닉 분석: 각 코드에 로마자 도수 표기 (I · ii · iii · IV · V · vi · vii°)
- 논다이아토닉 코드 해설
  - Secondary Dominant: V/x 표기
  - Borrowed Chord (Modal Interchange): 출처 모드 표기
  - Tritone Substitution: 감지 시 표기
- 각 코드 구성음 표시 (Root·3rd·5th·7th)
- 어울리는 스케일 자동 추천 (코드셋 구성음 포함율로 순위 산출)

**연동 버튼**
- `코드 보이싱 보기 →` : 선택 코드 → 탭2로 전달
- `솔로 메이킹 →` : 감지된 키 → 탭3으로 전달

---

### 🎼 탭 2 — 코드 보이싱

**목적**: 코드의 기타 운지법(보이싱)을 시각적으로 확인

**입력**
- 코드명 직접 검색
- 탭1 연동으로 자동 입력

**출력**
- SVG 기타 다이어그램 (6줄 × 5프렛)
  - 반응형: 가로/세로 전환 시 SVG 좌표 자동 재계산
  - 왼손잡이 반전 모드
  - 운지점 세로선(string) 위 정확한 위치 표시
  - 개방현 O, 뮤트 X 표시
  - 바레 코드 시각화
  - 구성음 색상 구분 (Root·3rd·5th·7th 각각 다른 색)
- 같은 코드 다중 보이싱 슬라이드 선택 (최소 3가지 포지션)
- 코드 구성음 및 음정 표시

**연동 버튼**
- `코드차트에 추가 →` : 선택한 보이싱 → 탭7 코드차트 마디에 추가

---

### 🎯 탭 3 — 솔로 메이킹

**목적**: 입력된 키/코드에서 어떤 스케일로 솔로를 만들지 가이드

**입력**
- 키 또는 코드 직접 입력
- 탭1 연동으로 자동 입력

**출력**
- 사용 가능한 스케일 목록 + 추천 순위 (구성음 포함율 기준)
- 기타리스트 스타일 필터 (5인 선택 시 해당 스타일 접근법으로 설명 변환)
- 선택 스케일의 포지션별 다이어그램 (SVG, 5포지션 CAGED)
- 어프로치 노트 제안
- 아르페지오 패턴 표시

**연동 버튼**
- `스타일 라이브러리 보기 →` : 선택한 기타리스트 → 탭5로 이동

---

### 🔍 탭 4 — 솔로 분석

**목적**: 유명 솔로를 이론적으로 해부하여 학습

**콘텐츠 구조 (정적 데이터)**
- 솔로별: 전체 구조 분해 (Intro·A섹션·B섹션·클라이맥스·Outro)
- 각 섹션: 사용 스케일·포지션 다이어그램 (SVG)
- 핵심 기법 해설 (기법명 + 설명 + 해당 마디)
- 대표 릭 TAB 표기 (ASCII TAB)

**연동 버튼**
- `스타일 라이브러리 보기 →` : 해당 아티스트 → 탭5로 이동

---

### 🌟 탭 5 — 스타일 라이브러리

**목적**: 5인 기타리스트의 스타일·기법·릭을 레퍼런스로 제공

**데이터 관리**: `data/guitarists.js`에 정적 JSON 객체로 분리. 탭5는 이를 읽어 렌더링만 함.

#### Nick Johnston
- **장르**: 네오클래식·멜로딕 퓨전
- **핵심 기법**: 레가토, 스윕 피킹, 스트링 스키핑, 탭핑
- **대표 스케일**: 하모닉 마이너, 리디안, 멜로딕 마이너
- **대표 릭**: 6개 이상 (ASCII TAB + 기법 설명 + 사용 스케일 표시)

#### Mark Lettieri
- **장르**: 펑크·R&B·재즈 퓨전
- **핵심 기법**: 하이브리드 피킹, 컴핑, 어퍼스트럭처 트라이어드
- **대표 스케일**: 도리안, 믹솔리디안, 펜타토닉
- **대표 릭**: 6개 이상

#### Guthrie Govan
- **장르**: 퓨전·블루스·재즈·컨트리·록
- **핵심 기법**: 치킨피킹, 레가토, 탭핑, 와미바, 크로매틱
- **대표 스케일**: 전 스케일 자유자재, 크로매틱 어프로치
- **대표 릭**: 6개 이상

#### Joe Satriani
- **장르**: 인스트루멘탈 록·퓨전
- **핵심 기법**: 레가토, 와미바 다이브, 비브라토, 스피드 피킹
- **대표 스케일**: 리디안 (시그니처), 펜타토닉, 이그조틱
- **대표 릭**: 6개 이상

#### John Mayer
- **장르**: 블루스·록·팝
- **핵심 기법**: 썸 테크닉, 하이브리드 피킹, 코드-멜로디
- **대표 스케일**: 마이너 펜타토닉, 도리안, 믹솔리디안
- **대표 릭**: 6개 이상

---

### 📂 탭 6 — 악보 보관함

**목적**: PDF·이미지 악보를 브라우저에 저장하고 관리

**저장 구조**
- 파일 원본: IndexedDB (용량 제한 없음, 브라우저 할당량 내)
- 메타데이터: localStorage (곡명·태그·폴더·BPM·키 등)
- 메타/파일 분리로 검색·필터는 빠르게, 파일 로드는 필요 시에만

**기능**
- 업로드: PDF, PNG, JPG, JPEG
- PDF 렌더링: PDF.js + `workers/pdf.worker.js` (메인 UI 스레드 프리징 완전 차단)
- 메타데이터 입력: 곡명·아티스트·장르·키·BPM·태그
- 썸네일 자동 생성 (PDF 1페이지)
- 검색·필터 (곡명·태그·장르·키·BPM)
- 폴더 분류 (사용자 정의)
- 악보 클릭 → 팝업 상세 뷰 (메타 수정·삭제)

**데이터 안정성 (최우선)**
- iOS Safari는 7일 미사용 시 IndexedDB 자동 삭제 가능
- 대응: 앱 진입 시 "마지막 백업으로부터 N일 경과" 배너 노출
- 전체 데이터(파일 포함) ZIP 내보내기 / 가져오기 기능

**연동 버튼**
- `코드차트 만들기 →` : 선택 악보 곡 정보 → 탭7 에디터에 자동 입력
- `라이브 모드에 추가 →` : 선택 악보 → 탭8 셋리스트에 추가

---

### 📝 탭 7 — 코드차트 (악보 축약기)

**목적**: 긴 악보를 1장짜리 코드차트로 축약

**에디터**
- 곡 정보: 곡명·키·박자·템포
- 섹션 추가 (Intro·Verse·Chorus·Bridge·Outro·Solo 등)
- 마디별 코드 입력 (박자 비율 자동 배치)
- 솔로 섹션: 이론 메모 입력란

**출력**
- 실시간 미리보기
- PNG로 저장 (html2canvas)
- 인쇄 버튼

**연동 버튼**
- `악보 보관함에 저장 →` : 완성된 차트 PNG → 탭6에 저장
- `라이브 모드에 추가 →` : 해당 코드차트 → 탭8 셋리스트에 추가
- 탭2 연동으로 보이싱 자동 입력 가능

---

### 🎬 탭 8 — 라이브 모드

**목적**: 공연 중 악보를 빠르고 편하게 넘기는 풀스크린 뷰어

**셋리스트 구성 화면**
- 탭6·탭7에서 추가된 악보/코드차트 목록
- 드래그 앤 드롭 순서 변경
- 셋리스트 JSON 내보내기/불러오기

**풀스크린 뷰어 (세로 Portrait 기준)**
- 전체화면 악보 표시
- 상단 헤더 3초 후 자동 숨김
- 하단 이전/다음 네비게이션 버튼 고정 (터치 영역 최대화)
- 좌우 스와이프로 곡 넘기기
- Wake Lock API 적용 (화면 꺼짐 방지)
  - 미지원 브라우저 대응: 1초마다 빈 영상 재생 트릭으로 화면 유지 시도
  - 사용자에게 미지원 여부 명확히 안내

---

### ⚙️ 탭 9 — 설정

**테마 (8종)**: 다크 프로, 라이트 클린, 블루 재즈, 빈티지 앰프, 포레스트, 네온 퓨전, 선셋 록, 페이퍼  
→ CSS 커스텀 프로퍼티(`--color-*`) 교체 방식. `body` `data-theme` 속성으로 전환.

**폰트 설정**
- 크기 조절 슬라이더 (80%~130%), `font-size` on `<html>` → rem 단위 전체 반영
- TAB 악보 폰트는 `font-family: monospace` 고정 (비율 변경 시에도 유지)

**방향 설정**
- 기본 세로 / 가로 고정 모드 (라이브 모드 진입 시 자동 Portrait 전환)

**데이터 백업/복원**
- 전체 내보내기: 메타데이터 JSON + 파일 원본 → ZIP 다운로드
- 전체 가져오기: ZIP 업로드 → 복원
- 수동 초기화 (2단계 확인)

---

## 6. 데이터 구조

### localStorage 키

| 키 | 타입 | 내용 |
|---|---|---|
| `gta_settings` | Object | 테마·폰트크기·방향 |
| `gta_sheet_meta` | Array | 악보 메타데이터 배열 (파일 원본 제외) |
| `gta_folders` | Array | 폴더 목록 |
| `gta_setlists` | Array | 저장된 셋리스트 목록 |
| `gta_chart_drafts` | Array | 코드차트 임시저장 목록 |
| `gta_last_backup` | String | 마지막 백업 날짜 (ISO) |
| `gta_current_analysis` | Object | 탭1 마지막 분석 결과 (탭 전환 복구용) |

### IndexedDB 스키마

**DB명**: `GuitarTheoryApp`  
**Object Store**: `sheets`

| 필드 | 타입 | 내용 |
|---|---|---|
| `id` | String (UUID) | 고유 ID |
| `file` | Blob | 파일 원본 (PDF·PNG·JPG) |
| `type` | String | "pdf" \| "image" \| "chart" |
| `thumbnail` | Blob | 썸네일 이미지 (PNG) |
| `createdAt` | Number | Unix timestamp |

---

## 7. UI/UX 설계

### 반응형 레이아웃

| 환경 | 레이아웃 |
|---|---|
| 모바일 (< 768px) | 하단 탭바 (엄지 영역 최적화, 터치 44×44px 이상, `env(safe-area-inset-bottom)` 대응) |
| PC (≥ 768px) | 좌측 사이드 탭바 + 메인 콘텐츠 (`max-width: 1200px`) |

### DOM 최적화

- 비활성 탭은 DOM 내부 HTML을 비움 (메모리 절약)
- 탭 전환 시점에만 해당 탭 JS 모듈의 `render()` 함수 호출 (조건부 렌더링)
- SVG 다이어그램은 렌더링 후 캐시 (같은 코드 재요청 시 재계산 없이 반환)

### 연동 UX 패턴

- 탭 내 연동 버튼 색상: 보조 강조색 (Primary 버튼과 구분)
- 연동 이동 시 원래 탭으로 돌아오는 뒤로가기 지원
- 연동으로 채워진 입력값은 연동 출처 배지 표시 (예: `← 탭1에서 가져옴`)

---

## 8. 음악이론 엔진 설계 (`theory.js`)

### API 불필요 근거

음악이론은 **수학적 규칙 집합**이다. 키 감지·다이아토닉 분석·스케일 추천 모두 알고리즘으로 완전 구현 가능.

### 핵심 알고리즘

**키 감지**
1. 모든 장조/단조(24개) 키에 대해 해당 키의 다이아토닉 코드 목록 계산
2. 입력된 코드 진행과 각 키의 다이아토닉 코드 교집합 크기 계산
3. 교집합이 가장 큰 키를 감지 키로 선택
4. 동점 시: 마지막 코드가 I 또는 V인 키 우선

**다이아토닉 분석**
- 12음 반음계 기준 각 코드의 루트 음정 계산
- 감지된 키의 7음 도수(scale degree)와 매칭
- 코드 타입(maj/min/dom/dim/aug)으로 도수 검증

**논다이아토닉 판별**
- Secondary Dominant: 다음 코드의 V7인지 확인
- Borrowed Chord: 같은 루트의 병행 조(Parallel Mode)에 존재하는지 확인
- Tritone Substitution: 원래 도미넌트에서 tritone 거리인지 확인

**스케일 추천**
- 각 스케일의 구성음과 코드 진행 전체 구성음 교집합 비율 계산
- 80% 이상 포함 시 "적합", 100% 시 "완전 일치"로 분류
- 스타일 필터 적용 시 해당 기타리스트 선호 스케일 가중치 부여

---

## 9. 구현 순서 (권장)

단계별로 완성 후 다음 단계 진행. 각 단계 완료 시 GitHub Pages에 배포하여 실제 기기에서 검증.

| 단계 | 작업 | 완료 기준 |
|---|---|---|
| 1 | 프로젝트 기본 틀 | `index.html` + CSS 레이아웃 + 9개 탭 전환 동작 |
| 2 | 음악이론 엔진 | `theory.js` 알고리즘 + 탭1 UI |
| 3 | 코드 보이싱 | `voicing.js` + SVG 다이어그램 + 탭2 UI |
| 4 | AppState 연동 | 탭1↔탭2↔탭3 실시간 연동 동작 |
| 5 | 솔로 메이킹 | `solo.js` + 탭3 UI |
| 6 | 스타일 라이브러리 | `data/guitarists.js` 데이터 작성 + 탭5 UI |
| 7 | 솔로 분석 | `analysis.js` 데이터 + 탭4 UI |
| 8 | 악보 보관함 | `db.js` + `sheets.js` + IndexedDB + PDF.js + 탭6 UI |
| 9 | 코드차트 | `chart.js` + PNG 출력 + 탭7 UI |
| 10 | 라이브 모드 | `live.js` + Wake Lock + 풀스크린 + 탭8 UI |
| 11 | 설정·백업 | `settings.js` + 테마 + ZIP 백업 + 탭9 UI |
| 12 | 탭 간 전체 연동 | 모든 연동 버튼 + AppState 전체 흐름 검증 |
| 13 | 마무리 | 모바일·PC 반응형 점검, iOS Safari 검증, 성능 최적화 |
